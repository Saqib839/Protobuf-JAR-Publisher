<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>datapipeline</groupId>
  <artifactId>proto-artifact</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <build>
    <extensions>
      <!-- Required for protobuf plugin -->
      <extension>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>1.7.0</version>
      </extension>
    </extensions>

    <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-compiler-plugin</artifactId>
      <version>3.10.1</version>
      <configuration>
        <source>21</source>
        <target>21</target>
      </configuration>
    </plugin>
    <plugin>
      <groupId>org.xolstice.maven.plugins</groupId>
      <artifactId>protobuf-maven-plugin</artifactId>
      <version>0.6.1</version>
      <configuration>
        <protocArtifact>com.google.protobuf:protoc:3.21.12:exe:${os.detected.classifier}</protocArtifact>
      </configuration>
      <executions>
        <execution>
          <goals>
            <goal>compile</goal>
            <!-- <goal>compile-custom</goal> -->
          </goals>
        </execution>
          <!-- Optionally, if you need gRPC or custom code generation -->
          <execution>
          <id>compile-grpc</id>
          <goals>
              <goal>compile-custom</goal>
          </goals>
          <configuration>
              <pluginId>grpc-java</pluginId>
              <pluginArtifact>io.grpc:protoc-gen-grpc-java:1.45.1:exe:${os.detected.classifier}</pluginArtifact>
          </configuration>
          </execution>
      </executions>
    </plugin>
    </plugins>
  </build>

  <dependencies>
    <!-- Add any protobuf runtime dependency if needed -->
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
      <version>3.21.12</version>
    </dependency>
  </dependencies>

  <!-- GitHub Packages Distribution Management -->
  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Sentinel-Codes Apache Maven Packages</name>
      <url>https://maven.pkg.github.com/Sentinel-Codes/Packages-Protobuf-Java</url>
    </repository>
  </distributionManagement>
</project>
